[variables]
# Keep the client output path explicit for the static production server.
NIXPACKS_SPA_OUTPUT_DIR = "dist/client"
# Coolify injects this into the runtime; the server also defaults to 3000 locally.
PORT = "3000"
